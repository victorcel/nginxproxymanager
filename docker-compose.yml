services:
  app:
    container_name: proxy-manager
    image: 'jc21/nginx-proxy-manager:latest'
    restart: unless-stopped
    ports:
      - '80:80'
      - '81:81'
      - '443:443'
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt
    networks:
      - main-external-network
      - botmax-external-network
networks:
    main-external-network:
      external: true
    botmax-external-network:
        external: true